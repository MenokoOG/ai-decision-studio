## 2025-05-15 - [Destructive Action Semantics]
**Learning:** Destructive actions like 'Remove' or 'Delete' should always use semantic red colors (`destructive` variant) and distinct iconography to prevent accidental data loss and improve accessibility.
**Action:** Use the `destructive` button variant with a `Trash2` icon and clear `aria-label` for all removal tasks.
