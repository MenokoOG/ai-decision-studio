# Handoff Snapshot - 2026-05-01

## Session Objective

Start implementation of the approved continuation plan:

- first-class snapshot/readiness persistence,
- deterministic formula acceptance hardening,
- plain-English guidance in web workflow,
- readiness parity in markdown export.

## What Changed

1. Deterministic calculator test hardening

- Expanded formula tests in `packages/calculators/src/businessCase.test.ts`.
- Added explicit checks for:
  - Year 1 one-time + annual behavior,
  - Year 2+ annual-only behavior,
  - cumulative running totals,
  - ROI null handling when denominator is zero,
  - payback null handling when annual net benefit is not positive.

2. Prisma schema + migration scaffolding for first-class persistence

- Updated `packages/db/prisma/schema.prisma`:
  - `Initiative.readinessJson` added,
  - `Initiative.confidenceScore` added,
  - new `BusinessCaseSnapshot` model added.
- Added migration SQL:
  - `packages/db/prisma/migrations/20260501121500_snapshot_readiness_first_class/migration.sql`.

3. API readiness + snapshot contract upgrades

- Added DTO: `apps/api/src/modules/initiatives/dto/save-readiness.dto.ts`.
- Updated controller: `apps/api/src/modules/initiatives/initiatives.controller.ts`:
  - `GET /api/v1/initiatives/:id/readiness`
  - `PATCH /api/v1/initiatives/:id/readiness`
- Updated service: `apps/api/src/modules/initiatives/initiatives.service.ts`:
  - readiness scoring helpers centralized,
  - `saveWorkspaceState` now persists readiness/confidence to initiative,
  - `getConfidence` now prefers first-class readiness and falls back to workspace state,
  - first-class snapshot create/list with legacy audit fallback,
  - dual-write snapshot audit event retained for audit traceability.

4. Export parity improvement

- Updated `packages/exporters/src/markdown.ts`:
  - optional readiness payload in export input,
  - readiness section in output,
  - confidence score included in export summary.

5. Web plain-English guidance pass

- Updated worksheet descriptions in `apps/web/src/lib/business-case.ts`.
- Updated readiness descriptions in `apps/web/src/lib/readiness.ts`.
- Added helper copy in `apps/web/src/app/page.tsx` for:
  - quick estimate fields,
  - usage scaling inputs,
  - scope/baseline inputs,
  - one-time vs annual meaning,
  - readiness status semantics,
  - ROI/payback explanations in summary.

## Validation Completed

1. Prisma client regenerated successfully.

- Command: `pnpm --filter @ai-cost-tool/db db:generate`

2. Typecheck succeeded.

- `pnpm --filter api typecheck`
- `pnpm --filter web typecheck`
- `pnpm --filter desktop typecheck`

3. Calculator tests executed directly and passed.

- Command: `pnpm dlx tsx --test packages/calculators/src/businessCase.test.ts`
- Result: 5 passing tests, 0 failures.

## Working Tree Notes

The workspace contains unrelated and/or pre-existing untracked content (for example `.github/`, course artifacts, and older handoff files now showing as untracked). Current implementation touched only the files listed under "What Changed" plus docs updates.

Typecheck commands also updated build info files:

- `apps/web/tsconfig.tsbuildinfo`
- `apps/desktop/tsconfig.tsbuildinfo`

## Immediate Next Steps (Resume Checklist)

1. Apply migration and verify DB schema.

- `pnpm --filter @ai-cost-tool/db db:migrate`

2. Smoke-test API endpoints in local dev runtime.

- `GET /api/v1/initiatives/:id/readiness`
- `PATCH /api/v1/initiatives/:id/readiness`
- `GET /api/v1/initiatives/:id/snapshots`
- `POST /api/v1/initiatives/:id/snapshots`
- `GET /api/v1/initiatives/:id/confidence`

3. Add idempotent backfill script.

- Migrate historical `AuditEvent` snapshot payloads into `BusinessCaseSnapshot`.

4. Optional but recommended cleanup before commit.

- Decide whether to commit or discard tsbuildinfo file changes.
- Stage only implementation-relevant files and docs.

## Suggested Restart Prompt

"Continue from docs/agent/handoff-2026-05-01.md. Apply migration, add snapshot backfill from AuditEvent to BusinessCaseSnapshot, then run endpoint smoke tests and update docs/agent/tasks.md with verification results."
