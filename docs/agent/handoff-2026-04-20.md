# Handoff Snapshot - 2026-04-20

## Session Objective

Stabilize web initiative persistence after runtime API 500s, improve save-state transparency, and keep agent logs current.

## What Changed

1. Fixed runtime API failures causing project save failures.
- Root cause: Nest controller/service injections resolved to `undefined` in runtime path.
- Result: `/api/v1/initiatives` and `/api/v1/business-case/preview` stopped throwing 500s.

2. Verified persistence flow end-to-end.
- Initiative create/list works.
- Workspace state save/load works.
- Business-case preview works.
- Snapshot save endpoint remains functional.

3. Improved save-state UX in web workspace.
- Auto-open most recent initiative after list load (when none selected).
- Added unsaved changes chip.
- Added autosave status chip (`idle`, `pending`, `saving`, `saved`, `error`).
- Added inline spinner on manual "Save Draft".
- Added subtle animated autosave dot while autosave is running.

4. Updated customer-facing documentation and presentation.
- Refreshed root README to match the active stack (`apps/web` + `apps/api` + Prisma + calculators).
- Removed coding-agent onboarding block from README.
- Removed readiness notes textareas from the checklist UX to keep interactions cleaner.
- Applied a stronger premium visual treatment in web UI (hero copy, card surfaces, atmospheric background, display typography).

5. Expanded premium UI pass for testing readiness.
- Added top-level executive KPI strip (workspace status, readiness completion, confidence, snapshots).
- Elevated workflow step cards with clearer active-state styling and step indexing.
- Upgraded readiness section to dashboard-style status cards and two-column checklist layout.
- Refined summary panel into action-oriented guidance cards for easier executive interpretation.

6. Addressed user confusion around calculations and app comprehension.
- Corrected deterministic formula parity: Year 1 now applies one-time + annual (Years 2+ remain annual), matching source worksheet pattern.
- Added explicit calculator discoverability and a second calculate button lower on page to avoid long-scroll friction.
- Added product-level Use Cases section and full Help section explaining step-by-step usage and formula behavior.

## Files Updated This Session

- `apps/api/src/modules/business-case/business-case.controller.ts`
- `apps/api/src/modules/initiatives/initiatives.controller.ts`
- `apps/api/src/modules/initiatives/initiatives.service.ts`
- `apps/web/src/app/page.tsx`
- `docs/agent/tasks.md`
- `docs/agent/memory.md`
- `docs/agent/handoff-2026-04-20.md`

## Validation

- `pnpm --filter api typecheck` passes.
- `pnpm --filter web typecheck` passes.
- API checks returned success for:
  - `GET /api/v1/initiatives`
  - `POST /api/v1/initiatives`
  - `PATCH /api/v1/initiatives/:id/workspace-state`
  - `POST /api/v1/business-case/preview`

## Remaining Focus

1. Promote event-log based snapshots/readiness into dedicated Prisma tables.
2. Add export-ready stakeholder outputs from persisted web initiative/snapshot data.
3. Continue web-first parity against `original-worksheets` artifacts.

## Suggested Next Prompt

"Continue from handoff 2026-04-20 and migrate workspace snapshot/readiness from audit-event payloads to first-class Prisma models and API responses, then update docs/agent/tasks.md."

## Follow-On File

- `docs/agent/follow-on-2026-04-20-monetization.md`
