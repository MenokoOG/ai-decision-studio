# GUARDRAILS

- AI cannot silently change financial values.
- Calculators must be deterministic and testable.
- Client apps must access persistence and AI through typed API/service boundaries.
- All write APIs require server-side validation.
- Secrets must never be stored in plain text source files.
- Every AI action must have an audit path.
- Keep forms progressive and readable.
